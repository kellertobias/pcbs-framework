/**
 * Placeholder for KiCad library types.
 * Consumers should override this via tsconfig paths to @tobisk/pcbs/kicad-types
 */

export type KicadLibrarySymbol = string;
export type KicadLibraryFootprint = string;
